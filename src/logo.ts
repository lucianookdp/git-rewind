// Two chevrons pointing back, echoing both "rewind" and the progress
// segments used through the story. currentColor so it can track the
// idle accent cycle and, later, the language accent.
export const LOGO_MARK_SVG = `
<svg viewBox="0 0 48 32" aria-hidden="true">
  <path d="M22 3 L22 29 L1 16 Z" fill="currentColor" />
  <path d="M47 3 L47 29 L26 16 Z" fill="currentColor" />
</svg>
`.trim()
